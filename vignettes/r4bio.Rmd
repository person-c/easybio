---
title: "r4bio"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{r4bio}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(easybio)
```


```{css}
h1 {font-style: italic}
h2 {font-style: italic}
body {font-size: 20px;
font-family: "Times New Roman", Times, serif;}

a {font-style: italic}

```


You can use cross reference for tables, figures, and expressions. @fig-cor shows the cor plot of mtcars dataset. If you want to cross-reference, those elements should have labels, because references dependent on these label environments, and the figure should have a caption(`fig.cap`)

## Correlation plot

```{r}
#| label: fig-cor
#| fig-cap: "Temperature and ozone level."
#| warning: false


plot(mtcars)
```
